<h1>Editing post</h1>

<%= form_for(@post) do |f| %>
  <%= render 'form', f: f %>
  <div class="actions form-group">
    <%= f.submit class: "btn btn-default" %>
  </div>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
